import { Routes } from '@angular/router';
import { TableFormComponent } from './table-form-multi-field/table-form.component';

export const formRoute: Routes = [
    { path: 'form/table-form', component: TableFormComponent },
];
